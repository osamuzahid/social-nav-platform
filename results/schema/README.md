# metrics_cited.csv

Example result rows copied from the dissertation campaign archives
(`museum_reachy_*`, `hospital_stretch_*`). Schema is HuNav evaluator output
plus hop-outcome fields used in those citeable CSVs.

Do not treat these copies as a new capture. Do not recapture hops.
