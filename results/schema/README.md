# metrics_cited.csv

Example result rows copied from the dissertation campaign archives
(`results/eval/<world>_<robot>_<stack>/metrics_cited.csv` for all fourteen
hops). Schema is HuNav evaluator output plus hop-outcome fields used in
those citeable CSVs.

Do not treat these copies as a new capture. Do not recapture hops.
